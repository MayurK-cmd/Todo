const mongoose = require("mongoose");



mongoose.connect("")//Connects to MongoDB and creates a database
const todoSchema = mongoose.Schema({
    title: String,
    description: String,
    completed: Boolean
})

const todo = mongoose.model('todos', todoSchema);

module.exports = {
    todo
}